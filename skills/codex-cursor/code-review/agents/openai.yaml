interface:
  display_name: "Code Review"
  short_description: "Thorough review, fixes, and optional PR flow"
  default_prompt: "Use $code-review to review the working tree with high intensity. Add --fix, --commit, --push, or --pr when you want those follow-up actions."
