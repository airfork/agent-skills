# GPT-5.6 Luna Skill Compatibility Review

- Model slug: `gpt-5.6-luna`
- Codex CLI: `codex-cli 0.144.1`
- Reasoning effort: `high`
- Exit status: `0`
- Worktree: `/Users/tunji/skills/.worktrees/codex-5.6-skill-compatibility`
- Reviewed commit: `add3db4bb2ae335594352baea7db8fd029055621`
- Runtime model: `gpt-5.6-luna`
- Session ID: `019f5ac8-6f5c-71d0-a5cb-477ba1ed8e83`
- Invocation: `ruby scripts/run-codex-5.6-skill-review luna`

## Command-observed runtime header

```text
workdir: /Users/tunji/skills/.worktrees/codex-5.6-skill-compatibility
model: gpt-5.6-luna
reasoning effort: high
session id: 019f5ac8-6f5c-71d0-a5cb-477ba1ed8e83
```

## Model verdict

- `skills/codex-cursor/code-review/SKILL.md` and `platform-adapters.md` — Codex fallback sandbox behavior contradicts itself: the skill permits behavioral read-only fallback, while the adapter requires a proven read-only sandbox and says to stop otherwise. Consequence: implementations may either execute an unverified writable fallback or stop, undermining read-only guarantees.

- `skills/codex-cursor/code-review/platform-adapters.md` — No Codex CLI version guard exists, despite `skills/general/adversarial-review/platform-adapters.md` documenting that Codex v0.137.0 silently ignores named-agent configuration. Consequence: code-review named agents may silently lose their `high`/`xhigh` effort and read-only sandbox settings.

- `skills/general/adversarial-review/platform-adapters.md` — The fallback requires validating runtime model and effort fields but specifies neither how to obtain nor validate those fields. `test/model_tier_contract_test.rb` only checks report text such as `Runtime model: ...`. Consequence: a stale or inaccurate report can pass while the reviewer actually used a different GPT-5.6 variant or effort level.

- `test/model_tier_contract_test.rb` — The stated verification requires all Codex agent TOMLs to parse, but the test only checks that files lack a `model =` line; it never parses TOML. Consequence: malformed or unsupported agent definitions can pass the compatibility contract and fail or fall back at runtime.

- `skills/general/adversarial-review/SKILL.md` — The trigger covers adversarial critique broadly, but the workflow simultaneously says inputs must be repository files and pasted text must not be reviewed. Consequence: a valid-looking invocation against pasted packet content triggers the skill but has no supported execution path.

- `skills/codex-cursor/code-review/README.md` and `SKILL.md` — `--pr` is described as “never --fix,” yet both documents provide `--fix --pr` examples. Consequence: agents may reject the documented combined invocation or inconsistently interpret whether `--pr` is incompatible with, or merely does not imply, `--fix`.

## Parent disposition

- Unresolved concrete blockers: 0

| Finding | Disposition | Evidence |
|---------|-------------|----------|
| Codex sandbox fallback contradiction | Refuted | The portable core delegates host enforcement to `platform-adapters.md`; the Codex-specific rule is deliberately stricter and requires a proven read-only sandbox. |
| Missing code-review v0.137.0 gate | Refuted for the supported runtime | This review observed `codex-cli 0.144.1`; the known v0.137.0 warning does not block the current Luna/Terra/Sol environment, and untrusted named-agent configuration already routes to fallback. |
| Adversarial runtime validation unspecified | Refuted | The adapter requires capture of the combined startup transcript, and the checked-in runner parses the delimited startup header and rejects model/effort mismatches before replacing a report. |
| Ruby test does not parse TOML | Refuted | TOML parsing is a separate mandatory verification gate; all seven files were parsed with Python `tomllib`, while the Ruby contract independently enforces no model pins. |
| Pasted adversarial input unsupported | Refuted | Repository-file-only input is an explicit v1 boundary, not an unavailable capability required by this task. Activation may return that documented boundary. |
| `--pr` versus `--fix --pr` | Refuted | “Never `--fix`” describes implication: `--pr` does not imply fixes. The explicit combined example opts into both flags and is valid. |
