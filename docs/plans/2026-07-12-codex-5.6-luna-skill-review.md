# GPT-5.6 Luna Skill Compatibility Review

- Model slug: `gpt-5.6-luna`
- Codex CLI: `codex-cli 0.144.1`
- Reasoning effort: `high`
- Exit status: `0`
- Worktree: `/Users/tunji/skills/.worktrees/codex-5.6-skill-compatibility`
- Reviewed commit: `3d7f3a4f4d0b1d1139b6bb196c199bcc2eab4bb1`
- Runtime model: `gpt-5.6-luna`
- Session ID: `019f5a91-c1e5-7ab2-80ed-6106edc79dcd`
- Invocation: `ruby scripts/run-codex-5.6-skill-review luna`

## Command-observed runtime header

```text
workdir: /Users/tunji/skills/.worktrees/codex-5.6-skill-compatibility
model: gpt-5.6-luna
reasoning effort: high
session id: 019f5a91-c1e5-7ab2-80ed-6106edc79dcd
```

## Model verdict

- `test/model_tier_contract_test.rb` — `EXPECTED_HUMAN_ROWS` duplicates the model mapping instead of deriving it from `skills.yaml`, and validation uses `assert_includes`. Contradictory or stale README/CATALOG mappings can remain alongside the expected rows while tests pass, leaving model selection ambiguous.

- `test/model_tier_contract_test.rb` — provenance validation checks only that reports contain strings such as `Runtime model:`, the expected slug, CLI version, and exit status. It cannot prove those reviews actually ran under the claimed GPT-5.6 models. A fabricated or stale report can certify compatibility falsely.

- `test/model_tier_contract_test.rb` — assertions hard-code `codex-cli 0.144.1` and the absolute worktree `/Users/tunji/skills/.worktrees/codex-5.6-skill-compatibility`. The contract test fails in another worktree or after a valid CLI upgrade, preventing reproducible compatibility verification.

- `skills/codex-cursor/code-review/SKILL.md` and `platform-adapters.md` — the generic-subtask fallback says to spawn subtasks but does not require explicitly preserving the parent’s selected GPT-5.6 model. If the host’s generic dispatcher uses its default model, fallback reviewers silently run under a different model.

- `skills/codex-cursor/code-review/platform-adapters.md` — the Codex adapter recommends running review sessions with parent effort `high`, while `SKILL.md` defines `deep` subagents as `xhigh` or host maximum. Following the adapter for a deep review can produce a mixed-effort run whose parent preparation/synthesis is only high, making deep execution non-deterministic.
