# Adversarial Review - Codex 5.6 Skill Compatibility Implementation Plan

PASSED

## Findings

| ID | Category | Severity | Location | Summary | Resolution |
|----|----------|----------|----------|---------|------------|
| AR-001 | Ambiguity | MEDIUM | Task 1 contract test | Human-table regex did not bind the expected model to the Codex column. | Resolved with exact full-row contracts for README and catalog. |
| AR-002 | Omission | HIGH | Task 3 review packet | Model reviewers could skip mandatory package resources and metadata. | Resolved by enumerating the resources and requiring every tracked text file in both active packages. |
| AR-003 | Inconsistency | HIGH | Task 3 compatibility gate | Promotion criteria excluded trigger and instruction defects that reviewers were told to find. | Resolved by matching the gate to every approved evaluation dimension. |
| AR-004 | Omission | MEDIUM | Task 4 TOML verification | Validation did not assert role-specific reasoning-effort pins. | Resolved with an exact seven-agent effort map and set equality check. |
| AR-005 | Incorrect fact | MEDIUM | Task 4 retired-model search | Bare `rg` returned exit 1 when the desired result was no matches. | Resolved with explicit match, no-match, and search-error handling. |
| AR-006 | Ambiguity | HIGH | Task 3 blocker branch | A compatibility fix did not require model reviews to be rerun. | Resolved by requiring all three reports to be replaced after a fix before continuing. |
| AR-007 | Omission | HIGH | Task 1 and Task 4 guards | Installed Codex skill files were absent from the durable retired-model guard. | Resolved with manifest-driven active-package coverage plus a final scan of both package directories. |
| AR-008 | Inconsistency | MEDIUM | Task 3 evidence | Model-review evidence lived only in `/tmp`, contrary to the repo's durable handoff rule. | Resolved by writing and committing three repo-local model reports. |

## Metrics

- Review tier: default maximum-rigor pipeline with xhigh attackers and judge.
- Starting documents: design 97 lines / 541 words; plan 291 lines / 1,384 words.
- Revised plan: 349 lines / 1,737 words.
- TBD/TODO count: 0 before, 0 after.
- Coverage: 95% in the attack pass; 100% after resolution checks.
- Findings: 0 CRITICAL, 4 HIGH, 4 MEDIUM, 0 LOW.
- Resolution: 8 resolved, 0 rejected, 0 stuck.
- Fresh-context note: two collaboration attackers completed fresh-context passes. Additional fresh Codex CLI attackers stalled after read-only repository inspection and were terminated; the remaining enabled angles were completed in follow-up passes by the isolated attackers. The cull used a separate fresh GPT-5.6 Sol xhigh judge.

## Changelog

1. Replaced loose human-table regex guidance with exact expected rows.
2. Extended the contract guard to every file under manifest-enabled Codex skill packages.
3. Expanded each model review to the complete active code-review and adversarial-review packages.
4. Made the three model reports durable repo-local artifacts.
5. Broadened compatibility promotion criteria to all approved review dimensions.
6. Required all three model reviews to rerun after any compatibility fix.
7. Added exact named-agent reasoning-effort verification.
8. Corrected negative-search exit handling and included active skill directories.

## Rejected Findings

None.

## Open Questions

None.

## Compatibility Blocker Revision - 2026-07-13

PASSED

Terra's first live model-perspective review found that inherited Luna conflicted
with unconditional fast-model prohibitions in quality roles, and that the
catalog did not distinguish model tiers from code-review intensity tiers. The
implementation plan stopped at its compatibility gate and added Task 2.5.

The first revision review found three issues:

| ID | Severity | Summary | Resolution |
|----|----------|---------|------------|
| AR-R2-001 | HIGH | The negative contract missed the exact adversarial adapter wording. | Resolved with an exact assertion for each of the five active policy surfaces. |
| AR-R2-002 | MEDIUM | Positive postconditions did not cover every policy surface or the exact catalog header/row. | Resolved with five positive Codex-policy assertions and bound catalog assertions. |
| AR-R2-003 | HIGH | A blanket relative-quality rule would weaken non-Codex quality floors, outside the approved design. | Resolved with a Codex-only GPT-5.6 inheritance exception and explicit preservation of non-Codex prohibitions. |

Final resolution checks confirmed all three findings resolved with no new
findings. Task 2.5 now requires RED/GREEN coverage for every affected package
surface and preserves Claude, Gemini, Cursor, and UltraCode behavior.

## Luna Compatibility Revision - 2026-07-13

PASSED

Luna's first live perspective review surfaced one plausible policy defect: the
code-review tier required stronger reasoning effort while its unsupported-host
fallback still allowed unqualified inherited effort. The plan added Task 2.6
and stopped before further source edits.

Focused adversarial review found and resolved the following plan defects:

| ID | Severity | Summary | Resolution |
|----|----------|---------|------------|
| AR-R3-001 | HIGH | Positive fallback assertions could pass while obsolete permissive wording remained. | Added exact negative assertions for both obsolete statements. |
| AR-R3-002 | HIGH | Model reviews targeted the main checkout instead of the compatibility worktree. | Bound every run to the compatibility worktree. |
| AR-R3-003 | MEDIUM | Durable reports omitted the reviewed worktree and revision. | Added worktree and commit provenance requirements. |
| AR-R3-004 | MEDIUM | The provenance test was not included in the report commit. | Staged the test with the reports. |
| AR-R3-005 | HIGH | A remediation rerun could overwrite provenance without regenerating it. | Required provenance regeneration and focused verification after every rerun. |
| AR-R3-006 | MEDIUM | The UUID contract was one character short. | Corrected it to the full 36-character shape. |
| AR-R3-007 | HIGH | Manually prepended labels did not bind a verdict to the runtime-selected model. | Replaced manual provenance with a checked-in runner that validates Codex's command-observed startup header before publishing a report. |

Two independent focused reviewers completed the first resolution check. A
final fresh check of the runtime-binding redesign reported no surviving
findings. Task 2.6 and Task 3 may proceed.

## Luna Live-Review Blocker Revision - 2026-07-13

PASSED

Luna's first successful runtime-bound review found two executable-contract
defects: code-review did not explicitly define the path when named Codex agent
definitions were absent, and adversarial-review's report-only mode conflicted
with its mandatory convergence-oriented report shape. The plan stopped the
remaining model wave and added Task 2.7.

The focused attack found two false-GREEN gaps in the first revision:

| ID | Severity | Summary | Resolution |
|----|----------|---------|------------|
| AR-R4-001 | HIGH | The generic-agent fallback appeared only in the adapter, while the core Codex skill still required named agents. | Added the same exact fallback contract and test assertions to both the core skill and adapter. |
| AR-R4-002 | HIGH | Report-only still inherited a mandatory Findings table with a Resolution column. | Defined and tested a report-only findings schema without Resolution and omitted all resolution-only sections. |

Independent resolution checks confirmed both findings resolved with no new
blockers. Task 2.7 may proceed before the Luna, Terra, and Sol reports are
regenerated.

## Sol Live-Review Blocker Revision - 2026-07-13

PASSED

Sol's runtime-bound review found two surviving execution defects after parent
evidence checking: code-review assumed that its full finder/verifier roster
could launch simultaneously, and adversarial-review did not define a unique
terminal verdict for stuck low/medium findings. The plan stopped and added Task
2.8.

The focused attack found two plan gaps:

| ID | Severity | Summary | Resolution |
|----|----------|---------|------------|
| AR-R5-001 | MEDIUM | The Task 2.8 commit omitted the modified regression test. | Staged the contract with the policy changes. |
| AR-R5-002 | HIGH | Positive verdict assertions could pass while the old severity-specific rule remained. | Added an exact negative assertion and required removal of the obsolete sentence. |

Independent resolution checks found no surviving or new blockers. Task 2.8 may
proceed before the final three reports are regenerated.

## Luna Final-Review Blocker Revision - 2026-07-13

PASSED

Luna's next runtime-bound pass found two surviving operational gaps:
named-agent installation could still be read as a review-time prerequisite,
and PR workflows did not define a deterministic stop when GitHub CLI was
missing or unauthenticated. The plan stopped and added Task 2.9.

The first focused attack found one false-GREEN gap:

| ID | Severity | Summary | Resolution |
|----|----------|---------|------------|
| AR-R6-001 | HIGH | Positive optional-install assertions could coexist with the existing imperative install/copy instructions. | Added exact negative assertions and required both imperative instructions to be rewritten as user-initiated optional setup. |

Two focused reviewers completed the attack, and the resolution check found no
surviving or new blockers. Task 2.9 may proceed before final report generation.

## Luna Contract-Hardening Revision - 2026-07-13

PASSED

Luna's next packet review found two surviving contract gaps: human guidance
could contain a second contradictory tier row while expected-row assertions
still passed, and generic fallback subtasks did not explicitly preserve the
selected parent model. The plan stopped and added Task 2.10.

The focused attack found one scope defect:

| ID | Severity | Summary | Resolution |
|----|----------|---------|------------|
| AR-R7-001 | HIGH | A GPT-5.6-specific fallback sentence in the shared core skill would be impossible on Gemini or Cursor. | Split the rule into a host-neutral selected-model policy in the core and an explicit GPT-5.6 policy only in the Codex adapter. |

Independent resolution checks found no surviving or new blockers. Task 2.10
may proceed before the final model reports are regenerated.

## Installed-Copy And Fresh-Session Revision - 2026-07-13

PASSED

Luna's post-fix run showed that Codex auto-loaded the still-stale repo-backed
main-checkout skill alongside the worktree packet, and it found an independent
fresh-session example that pinned effort without the selected GPT-5.6 slug.
The plan stopped and added Task 2.11.

Focused review found and resolved three sequencing/coverage gaps:

| ID | Severity | Summary | Resolution |
|----|----------|---------|------------|
| AR-R8-001 | HIGH | The adjacent effort profile could still start a fresh default-model process. | Required an explicit selected-model invocation for both direct config and profile paths. |
| AR-R8-002 | HIGH | Main could be fast-forwarded before verification and drift after a later source fix. | Required a passing non-report verification gate before every fast-forward and before any report rerun. |
| AR-R8-003 | MEDIUM | Requiring report provenance in the pre-sync gate created a circular dependency on reports not yet regenerated. | Deferred only report provenance until after Task 3 while preserving every non-report gate before synchronization. |

Independent resolution checks found no surviving or new blockers. Task 2.11
may proceed.

### Worktree install-check note

The repo-managed global Codex skill links point to `/Users/tunji/skills`, so a
sync dry-run from the isolated worktree correctly classifies them as outside
that worktree's ownership. The pre-sync gate runs this one check from the clean
main checkout, where both links report `already linked`, and repeats it after
the fast-forward. No global link is applied or changed.

## Adversarial Fallback Model-Inheritance Revision - 2026-07-13

PASSED

Sol's final packet review found that the adversarial-review Codex fallback did
not prove that fresh attacker, judge, and arbiter contexts inherited the exact
selected GPT-5.6 model. The plan stopped and added Task 2.12.

Both focused reviewers rejected the first proposed inline-parent fallback:

| ID | Severity | Summary | Resolution |
|----|----------|---------|------------|
| AR-R9-001 | HIGH | Running a role inline in the parent would violate the core fresh-context invariant and could silently use lower-than-xhigh effort. | Replaced the proposal with a fail-closed Codex policy requiring proof of fresh context, the selected GPT-5.6 model, and xhigh or host-maximum effort. |
| AR-R9-002 | HIGH | The portable core still authorized continuing when xhigh effort was unavailable, contradicting the stricter Codex adapter. | Replaced the unconditional core downgrade with a host-neutral adapter-or-stop rule and added positive and negative contract assertions. |
| AR-R9-003 | MEDIUM | The task's commit step omitted the newly added core-skill edit. | Required the core skill, platform adapter, and contract test to be committed together before main is resynchronized. |

The revised task removes both permissive paths and requires Codex to stop with
an explicit unenforceable-tier result when any of the three role guarantees
cannot be proven. Task 2.12 may proceed.

## Final Cross-Model Interpretation Revision - 2026-07-13

PASSED

The refreshed Luna, Terra, and Sol packet runs exposed a bounded final set of
interpretation and regression-contract gaps. The plan stopped and added Task
2.13 before accepting or replacing the reports.

Focused review found and resolved four plan defects:

| ID | Severity | Summary | Resolution |
|----|----------|---------|------------|
| AR-R10-001 | HIGH | Prose-only assertions could false-green without testing parser precedence, shell fallback behavior, or structural inheritance guards. | Split coverage into independently named tests, parsed the alias table, added two executable git shell fixtures, and required structural TOML/GPT/runner checks. |
| AR-R10-002 | HIGH | Mapping ordinary critique to report-only still authorized an unrequested report-file write. | Made ordinary natural-language critique chat-only unless the user explicitly requests a report file, revision, or the full command workflow. |
| AR-R10-003 | HIGH | The fresh `codex exec ... -` example omitted transport of the role prompt and review packet. | Required a programmatic launcher to write the complete packet to stdin, close it, capture the startup transcript, and validate model and effort before accepting output. |
| AR-R10-004 | MEDIUM | The initial task did not make every intended behavior independently observable. | Added exact positive/negative contracts for effort, fallback precedence, catalog terminology, and write authorization. |

Independent resolution checks found no surviving or new blockers. Task 2.13
may proceed before the three final model reports are regenerated.
