# GPT-5.6 Sol Skill Compatibility Review

- Model slug: `gpt-5.6-sol`
- Codex CLI: `codex-cli 0.144.1`
- Reasoning effort: `high`
- Exit status: `0`
- Worktree: `/Users/tunji/skills/.worktrees/codex-5.6-skill-compatibility`
- Reviewed commit: `4d54cf52cf9392c9c8b966a827a2d33cb9e5c0cb`
- Runtime model: `gpt-5.6-sol`
- Session ID: `019f5a85-6650-79d1-bb18-864383a64e3b`
- Invocation: `ruby scripts/run-codex-5.6-skill-review sol`

## Command-observed runtime header

```text
workdir: /Users/tunji/skills/.worktrees/codex-5.6-skill-compatibility
model: gpt-5.6-sol
reasoning effort: high
session id: 019f5a85-6650-79d1-bb18-864383a64e3b
```

## Model verdict

1. **Active installed adversarial-review policy is stale**  
   Path: `/Users/tunji/skills/skills/general/adversarial-review/SKILL.md`  
   Evidence: the active copy says judges and arbiters “must never be downgraded to a fast or cheap model,” while the packet’s `skills/general/adversarial-review/SKILL.md` explicitly permits the selected parent GPT-5.6 model at every tier. `README.md` defines Luna as the `fast` model tier.  
   Consequence: a Luna session receives incompatible instructions—inherit Luna while also refusing a fast model—causing unnecessary escalation, refusal, or inconsistent disclosure.

2. **Active installed code-review policy is stale**  
   Path: `/Users/tunji/skills/skills/codex-cursor/code-review/SKILL.md`  
   Evidence: the active copy prohibits a `"fast"` verifier and says to continue with inherited settings when effort cannot be controlled. The packet’s tracked copy instead permits every parent GPT-5.6 model and requires maximum available effort when the requested effort cannot be enforced.  
   Consequence: Luna cannot satisfy both inheritance and the fast-model prohibition, while hosts lacking effort overrides may silently run `standard` or `deep` below their promised effort.

3. **The `fast` vocabulary conflates model and review tiers**  
   Paths: `README.md`; `skills/codex-cursor/code-review/SKILL.md`  
   Evidence: `README.md` defines `fast` as the GPT-5.6 Luna model tier, while code-review parses `fast` as an alias for the `quick` review structure. The approved design explicitly says model tiers and review intensities must not be conflated.  
   Consequence: “run code-review fast” can be interpreted as selecting Luna or as disabling subagents and verification via the quick tier.

4. **Code-review assumes more parallel capacity than this Codex host exposes**  
   Path: `skills/codex-cursor/code-review/SKILL.md`  
   Evidence: it requires all 8 or 10 finders—and later all location-group verifiers—to be dispatched in parallel. Its sequential fallback applies only when subagent tools do not exist. This run permits only three concurrent child agents. Unlike adversarial-review, code-review provides no bounded-wave behavior.  
   Consequence: `standard`, `high`, and `deep` cannot be executed literally; implementations may exceed the agent limit, fail dispatch, or invent inconsistent batching semantics.

5. **A plain adversarial “review” silently authorizes writes**  
   Path: `skills/general/adversarial-review/SKILL.md`  
   Evidence: its trigger covers ordinary requests to review a spec or plan, but the default workflow edits those documents during “Revise Or Reject” and always writes a report file. Only `--report-only` prevents document revision, and even that still writes a file.  
   Consequence: Codex may mutate files after a review-only request that did not authorize edits, conflicting with normal read-only review semantics.

6. **Adversarial-review has contradictory terminal verdict rules**  
   Path: `skills/general/adversarial-review/SKILL.md`  
   Evidence: it mandates `DID NOT CONVERGE` at the round cap whenever findings remain open, but also offers `PASSED WITH OPEN QUESTIONS` and separately says only stuck `CRITICAL` or `HIGH` findings prevent passing.  
   Consequence: there is no deterministic verdict for stuck `LOW` or `MEDIUM` findings; different GPT-5.6 runs can legitimately emit incompatible outcomes.

7. **The standard code-review precision contract is internally inconsistent**  
   Paths: `skills/codex-cursor/code-review/SKILL.md`; `skills/codex-cursor/code-review/verifier-rubric.md`  
   Evidence: standard finders are told every surfaced finding must be actionable, but also to pass every half-believed candidate without prefiltering. Verification then retains every `PLAUSIBLE` candidate and places the burden on refutation.  
   Consequence: the advertised precision-tuned standard tier cannot be interpreted consistently and can behave like a recall-biased review.
