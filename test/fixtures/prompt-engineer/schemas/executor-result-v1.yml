type: object
required: [schema_version, run_id, host, case_id, arm, repeat_index, nonce, public_task_packet_digest, arm_environment_manifest_digest, expected_package_digest, masked_label_map_digest, sandbox_launch_attestation_digest, activation_evidence, invocation_evidence, session, fresh_session_evidence, timestamps, cli, model, effort, configuration_digest, environment_digest, tool_inventory, messages, tool_events, final_status, exit_status, raw_export_digest, usage]
additionalProperties: false
properties:
  schema_version: {type: integer, const: 1}
  run_id: {type: string, minLength: 1}
  host: {type: string, enum: [codex, claude]}
  case_id: {type: string, pattern: '\APE-[0-9]{3}\z'}
  arm: {type: string, enum: [unassisted, legacy, replacement]}
  repeat_index: {type: integer, minimum: 0}
  nonce: {type: string, pattern: '\A[0-9a-f]{64}\z'}
  public_task_packet_digest: {type: string, pattern: '\A[0-9a-f]{64}\z'}
  arm_environment_manifest_digest: {type: string, pattern: '\A[0-9a-f]{64}\z'}
  expected_package_digest: {type: string, pattern: '\A[0-9a-f]{64}\z'}
  masked_label_map_digest: {type: string, pattern: '\A[0-9a-f]{64}\z'}
  sandbox_launch_attestation_digest: {type: string, pattern: '\A[0-9a-f]{64}\z'}
  activation_evidence: {type: object, required: [status, event_ordinal, staged_path, machine_id, binding, binding_digest, machine_binding_digest, evidence_digest], additionalProperties: false, properties: {status: {type: string, enum: [activated, not_activated, unavailable]}, event_ordinal: {type: integer, minimum: 0}, staged_path: {type: string, minLength: 1}, machine_id: {type: string, minLength: 1}, binding: {type: object, required: [arm, case_id, host, launch_attestation_digest, machine_id, nonce, public_task_packet_digest, raw_export_digest, run_id, session_id, staged_package_digest, staged_path], additionalProperties: false, properties: {arm: {type: string, enum: [unassisted, legacy, replacement]}, case_id: {type: string, pattern: '\APE-[0-9]{3}\z'}, host: {type: string, enum: [codex, claude]}, launch_attestation_digest: {type: string, pattern: '\A[0-9a-f]{64}\z'}, machine_id: {type: string, minLength: 1}, nonce: {type: string, pattern: '\A[0-9a-f]{64}\z'}, public_task_packet_digest: {type: string, pattern: '\A[0-9a-f]{64}\z'}, raw_export_digest: {type: string, pattern: '\A[0-9a-f]{64}\z'}, run_id: {type: string, minLength: 1}, session_id: {type: string, minLength: 1}, staged_package_digest: {type: string, pattern: '\A[0-9a-f]{64}\z'}, staged_path: {type: string, minLength: 1}}}, binding_digest: {type: string, pattern: '\A[0-9a-f]{64}\z'}, machine_binding_digest: {type: string, pattern: '\A[0-9a-f]{64}\z'}, evidence_digest: {type: string, pattern: '\A[0-9a-f]{64}\z'}}}
  invocation_evidence: {type: object, required: [status, event_ordinal, staged_path, machine_id, binding, binding_digest, machine_binding_digest, evidence_digest], additionalProperties: false, properties: {status: {type: string, enum: [invoked, not_invoked, unavailable]}, event_ordinal: {type: integer, minimum: 0}, staged_path: {type: string, minLength: 1}, machine_id: {type: string, minLength: 1}, binding: {type: object, required: [arm, case_id, host, launch_attestation_digest, machine_id, nonce, public_task_packet_digest, raw_export_digest, run_id, session_id, staged_package_digest, staged_path], additionalProperties: false, properties: {arm: {type: string, enum: [unassisted, legacy, replacement]}, case_id: {type: string, pattern: '\APE-[0-9]{3}\z'}, host: {type: string, enum: [codex, claude]}, launch_attestation_digest: {type: string, pattern: '\A[0-9a-f]{64}\z'}, machine_id: {type: string, minLength: 1}, nonce: {type: string, pattern: '\A[0-9a-f]{64}\z'}, public_task_packet_digest: {type: string, pattern: '\A[0-9a-f]{64}\z'}, raw_export_digest: {type: string, pattern: '\A[0-9a-f]{64}\z'}, run_id: {type: string, minLength: 1}, session_id: {type: string, minLength: 1}, staged_package_digest: {type: string, pattern: '\A[0-9a-f]{64}\z'}, staged_path: {type: string, minLength: 1}}}, binding_digest: {type: string, pattern: '\A[0-9a-f]{64}\z'}, machine_binding_digest: {type: string, pattern: '\A[0-9a-f]{64}\z'}, evidence_digest: {type: string, pattern: '\A[0-9a-f]{64}\z'}}}
  session: {type: object, required: [id, fresh], additionalProperties: false, properties: {id: {type: string, minLength: 1}, fresh: {type: boolean, const: true}}}
  fresh_session_evidence: {type: object, required: [new_session_marker, parent_session_absent, first_event_ordinal], additionalProperties: false, properties: {new_session_marker: {type: string, minLength: 1}, parent_session_absent: {type: boolean, const: true}, first_event_ordinal: {type: integer, minimum: 0}}}
  timestamps: {type: object, required: [started_at, ended_at], additionalProperties: false, properties: {started_at: {type: string, minLength: 1}, ended_at: {type: string, minLength: 1}}}
  cli: {type: object, required: [name, version, executable_digest], additionalProperties: false, properties: {name: {type: string, minLength: 1}, version: {type: string, minLength: 1}, executable_digest: {type: string, pattern: '\A[0-9a-f]{64}\z'}}}
  model: {type: string, minLength: 1}
  effort: {type: string, minLength: 1}
  configuration_digest: {type: string, pattern: '\A[0-9a-f]{64}\z'}
  environment_digest: {type: string, pattern: '\A[0-9a-f]{64}\z'}
  tool_inventory: {type: array, items: {type: string, minLength: 1}}
  messages: {type: array, minItems: 1, items: {type: object, required: [ordinal, channel, text], additionalProperties: false, properties: {ordinal: {type: integer, minimum: 0}, channel: {type: string, enum: [assistant, user, system]}, text: {type: string}}}}
  tool_events: {type: array, items: {type: object, required: [ordinal, tool, status], additionalProperties: false, properties: {ordinal: {type: integer, minimum: 0}, tool: {type: string, minLength: 1}, status: {type: string, enum: [started, completed, failed]}}}}
  final_status: {type: string, enum: [completed, failed, aborted, inconclusive]}
  exit_status: {type: integer, minimum: 0}
  raw_export_digest: {type: string, pattern: '\A[0-9a-f]{64}\z'}
  usage: {type: object, required: [input_tokens, output_tokens, total_tokens], additionalProperties: false, properties: {input_tokens: {type: integer, minimum: 0}, output_tokens: {type: integer, minimum: 0}, total_tokens: {type: integer, minimum: 0}}}
