type: object
required: [schema_version, roots]
additionalProperties: false
properties:
  schema_version: {type: integer, const: 1}
  roots: {type: array, minItems: 1, items: {type: object, required: [host, path, owner, device, inode], additionalProperties: false, properties: {host: {type: string, enum: [codex, claude]}, path: {type: string, pattern: '\A/'}, owner: {type: integer, minimum: 0}, device: {type: integer, minimum: 0}, inode: {type: integer, minimum: 0}}}}
