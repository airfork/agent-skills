type: object
required: [schema_version, roots]
additionalProperties: false
properties:
  schema_version: {type: integer, const: 1}
  roots: {type: array, minItems: 1, items: {type: object, required: [host, path, expected_owner, expected_mode, expected_device, expected_inode, expected_sha256], additionalProperties: false, properties: {host: {type: string, enum: [codex, claude]}, path: {type: string, pattern: '\A/'}, expected_owner: {type: integer, minimum: 0}, expected_mode: {type: integer, minimum: 0}, expected_device: {type: integer, minimum: 0}, expected_inode: {type: integer, minimum: 0}, expected_sha256: {type: string, pattern: '\A[0-9a-f]{64}\z'}}}}
