case_id: PE-004
title: Separate capability limits from wording defects
task: Decide whether a failure is caused by prompt text or model capability.
prompt_context: single_prompt
public_requirements: [diagnose_before_editing, report_unmeasured_limits, preserve_scope]
input_artifact_paths: [artifacts/input.txt]
required_host_configuration: {hosts: [codex, claude], fresh_context: true}
allowed_tools: [read]
time_budget: {seconds: 600}
profile: standard
primary_dimension: diagnosis_correctness
host_coverage: [codex, claude]
declared_worktree_inputs: [artifacts/input.txt]
safety_classification: evidence_limited
